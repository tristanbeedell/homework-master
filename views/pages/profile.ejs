<!DOCTYPE html>
<html>

<% title = `${user.displayName+(user.displayName.slice(-1)=='s'?"'":"'s")} Profile`%>
<% include ../partials/head %>

<body>
	<% if (user.id == member.id) { %>
	THIS IS YOUR PROFILE. <a href="/me">Customise</a>
	<% } %>
	<div class="header">
		<h1>
			<img src="<%= user.user.avatarURL %>" class="profilePic"></img>
			<div id="usernameContainer">
				<%= user.displayName%>
			</div>
		</h1>
	</div>
	<div class="content">
		<% if (userData.bio) { %>
		<div class="post">
			<div class="message"><%- userData.bio %></div>
		</div>
		<% } %>
		<div class="post">
			<div class="title">Features to come here?</div>
			<div class="message">
				If you have any ideas, please tell me what you think I should put here.<br>
				<h4>My ideas:</h4>
				User Timetable<br>
				Achievements<br>
				Custom colour<br>
				Social media links
			</div>
		</div>
	</div>
	<% include ../partials/menu %>
	<script src="/js/index.js"></script>
</body>

</html>