<!DOCTYPE html>
<html lang="en">

<% title = invite.guild.name %>
<% include ../partials/head %>

<body>
	<div class="header">
		<h1>You've been invited!</h1>
	</div>
	<div class="content">
		<div class="post invite" onclick="document.href = '<%= invite.url%>'">
			<div class="title">
				<%= group.school_name%>
				year
				<%= group.year%>
				<br>
				<a href="<%= invite.url%>">Click to open Discord</a>
				<br>
				Or use the invite code
				<%= invite.code%>
			</div>
			<div class="author">
				created by:
				<%= invite.guild.owner.nickname || invite.guild.owner.user.username%>
			</div>
			<div class="message">
				<%= invite.memberCount-1%> members so far!
				<p>
					<%= group.invite%>
				</p>
			</div>
		</div>
		<% let guild = invite.guild %>
		<% include ../partials/widget%>
	</div>
	<% include ../partials/menu %>
	<script src="/js/index.js"></script>
</body>

</html>